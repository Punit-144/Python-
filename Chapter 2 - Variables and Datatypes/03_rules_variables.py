# A variable name can contain alphabets, digits and underscore.
# A variable name can only start with an aplhabet and underscore
# A varianle name cannot start with a digit.
# No white space is allowed to be used inside a variable name.

a = 23 #valid

punit = 07 #valid

_punit = 22 #valid

# @punit = 11 is invalid since we can only use alphabets underscore and digits
# s@meer invalid due to @ symbol
