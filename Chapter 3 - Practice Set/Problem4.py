# Replace the double space from problem 3 to single space

name = "Punit is a good  boy and "

print(name.replace("  ", " ")) 

print(name) # Strings are immutable which means that you cannot change them by running functions on them